pipeline {
    agent any
    
    environment {
        Name= "cty"
    }
    
    stages {
        stage ('env') {
            steps {
                echo "$Name" & "Date"
            }
        }
    }
}
test
