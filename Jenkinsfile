pipeline {
    agent any
    
    environment {
        Name= "cytiva"
    }
    
    stages {
        stage ('env') {
            steps {
                echo "$Name"
            }
        }
    }
}
